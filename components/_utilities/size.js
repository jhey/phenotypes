// Values for phSize* props:
const SMALL = 'small';
const LARGE = 'large';

const isSmall = size => size === SMALL;
const isLarge = size => size === LARGE;

module.exports = {
  isSmall,
  isLarge,
};
